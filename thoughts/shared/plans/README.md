# Implementation Plans

This directory stores detailed implementation plans for v2 features.

Plans are created using the `/p_create_plan` command and should follow this naming convention:

- `feature-name.md` - descriptive kebab-case name

Example: `session-filtering.md`, `topic-categories.md`
